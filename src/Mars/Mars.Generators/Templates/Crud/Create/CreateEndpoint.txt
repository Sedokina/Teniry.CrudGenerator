using Microsoft.AspNetCore.Mvc;
using ITech.Cqrs.Cqrs.Commands;
using {{business_logic_namespace}};

namespace {{endpoint_namespace}};

public static partial class {{endpoint_class_name}}
{
    /// <summary>
    ///     Create new {{entity_name}}
    /// </summary>
    /// <response code="201">New {{entity_name}} created</response>
    [ProducesResponseType(typeof({{dto_name}}), 201)]
    public static async Task<IResult> CreateAsync(
        {{command_name}} command,
        ICommandDispatcher commandDispatcher,
        CancellationToken cancellation)
    {
        var result = await commandDispatcher
            .DispatchAsync<{{command_name}}, {{dto_name}}>(command, cancellation);
        
        return TypedResults.Created({{get_entity_route}}, result);
    }
}